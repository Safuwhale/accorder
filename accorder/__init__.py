"""Accorder — scrapes grant/funding portals, extracts structured data via a
hybrid deterministic + LLM pipeline, validates it, and tracks changes over
time. See ARCHITECTURE.md in the repo root for the full design."""

__version__ = "0.1.0"